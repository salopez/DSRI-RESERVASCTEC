@model Datos.DSRIFTIPOMOBILIARIO

@{
    ViewBag.Title = "Eliminar Tipo de Mobiliario";
    Layout = "~/Views/Shared/_Administrador.cshtml";
}

<h2>@ViewBag.Title</h2>

<h3>¿Está seguro que desea borrar el registro?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.NOM_TIPOMOBILIARIO)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NOM_TIPOMOBILIARIO)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Eliminar" class="btn btn-danger" /> |
            @Html.ActionLink("Regresar al contenido principal", "Index", null, new { @class = "btn btn-default" })
        </div>
    }
</div>
